@model IEnumerable<eventy.Models.Event>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    Event Name
                </th>
                <th>
                    Event Date
                </th>
                <th>
                    Max number of Families
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EventName)
            </td>
            <td>
                @{
                    var localizedTime = item.EventDate.ToLocalTime();
                }
                @Html.DisplayFor(modelItem => localizedTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaxNumberOfFamilies)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
